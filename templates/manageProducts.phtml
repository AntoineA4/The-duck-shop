<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="./assets/index.css">
    <link rel="stylesheet" href="./assets/manageProducts.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Copse&family=DynaPuff:wght@400..700&family=Gluten:wght@100..900&display=swap" rel="stylesheet">

    <title>Document</title>
</head>
<body>
    <header>
        <h1>My ducky world</h1>
        <nav id="main-nav">
            <ul id="nav-barre">
                <?php if (isset($_SESSION['user'])) : ?>
                    <li class="nav-user">
                        Bonjour <?= htmlspecialchars($_SESSION['user']['login']); ?>
                    </li>
                    <li>
                        <a href="logout.php" id="logout">Logout</a>
                    </li>
                <?php else : ?>
                    <li>
                        <a href="login.php">Login</a>
                    </li>
                <?php endif; ?>
                <li><a href="index.php">Accueil</a></li>
                <li><?php if (isset($_SESSION['user'])) : ?>
                        <a href="addProduct.php">Ajouter un produit</a>
                    <?php endif; ?></li>   
                <li><?php if (isset($_SESSION['user'])) : ?>
                        <a href="manageproduct.php">Gérer les produits</a>
                    <?php endif; ?></li>    
            </ul>
        </nav>
        <div class="header-right">
            <button id="burger-btn">
                <i class="fa-solid fa-bars"></i>
            </button>
        </div>
    </header>
    <main>
        <h2>Gestion des produits</h2>

        <div id="product-container">
            <?php foreach($products as $prod): ?>
            <div class="product_card">
                <!-- CROIX pour supprimer -->
                <a href="deleteProduct.php?id=<?= $prod['id'] ?>" class="delete-btn"
                onclick="return confirm('Êtes-vous sûr de vouloir supprimer ce produit ?');">✖</a>

                <div class="img_product">
                    <img src="<?= htmlspecialchars($prod['image']) ?>" 
                        alt="<?= htmlspecialchars($prod['titre']) ?>" 
                    >
                </div>
                <h3><?= htmlspecialchars($prod['titre']) ?></h3>
                <div class="info_product">
                    <p class="product-description"><?= htmlspecialchars($prod['descriptif']) ?></p>
                    <div class="price_and_cart"> 
                        <p class="product_price"><?= htmlspecialchars($prod['prix']) ?>€</p>
                        <!-- BOUTON MODIFIER -->
                        <button class="edit-btn" data-id="<?= $prod['id'] ?>"
                            data-titre="<?= htmlspecialchars($prod['titre']) ?>"
                            data-image="<?= htmlspecialchars($prod['image']) ?>"
                            data-descriptif="<?= htmlspecialchars($prod['descriptif']) ?>"
                            data-prix="<?= htmlspecialchars($prod['prix']) ?>">
                            Modifier
                        </button>
                    </div>
                </div>
            </div>
            <?php endforeach; ?>
        </div>

        <!-- MODAL pour modifier -->
        <div id="editModal" class="modal">
            <div class="modal-content">
                <span class="close">&times;</span>
                <h3 id="modal_title">Modifier le produit</h3>
                <form id="editForm" method="POST" action="manageproduct.php">
                    <input type="hidden" name="id" id="modal-id">
                    <label class="modal-contant">Titre :</label>
                    <input type="text" name="titre" id="modal-titre" class="modal-input" required>

                    <label class="modal-contant">Image (URL) :</label>
                    <input type="text" name="image" id="modal-image" class="modal-input" required>

                    <label class="modal-contant">Descriptif :</label>
                    <textarea name="descriptif" id="modal-descriptif" class="modal-input" required></textarea>

                    <label class="modal-contant">Prix :</label>
                    <input type="number" step="0.01" name="prix" id="modal-prix" class="modal-input" required>

                    <button type="submit" class="modal-contant" id="submit_change">Enregistrer</button>
                </form>
            </div>
        </div>
</main>
    <footer>
        <ul>
            <li><a href="#">Contact</a></li>
            <li><a href="#">À propos</a></li>
            <li><a href="#">mentions légales</a></li>
        </ul>
    </footer>  
<script src="./assets/manageProducts.js"></script> 
<script src="./assets/menuBurger.js"></script>
</body>
</html>