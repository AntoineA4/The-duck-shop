<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="./assets/index.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Copse&family=DynaPuff:wght@400..700&family=Gluten:wght@100..900&display=swap" rel="stylesheet">

    <title>Document</title>
</head>
<body>
    <header>
        <h1>My ducky world</h1>
        <nav id="main-nav">
            <ul id="nav-barre">
                <?php if (isset($_SESSION['user'])) : ?>
                    <li class="nav-user">
                        Bonjour <?= htmlspecialchars($_SESSION['user']['login']); ?>
                    </li>
                    <li>
                        <a href="logout.php" id="logout">Logout</a>
                    </li>
                <?php else : ?>
                    <li>
                        <a href="login.php">Login</a>
                    </li>
                <?php endif; ?>
                <li><a href="index.php">Accueil</a></li>
                <li><?php if (isset($_SESSION['user'])) : ?>
                        <a href="addProduct.php">Ajouter un produit</a>
                    <?php endif; ?></li>   
                <li><?php if (isset($_SESSION['user'])) : ?>
                        <a href="manageproduct.php">Gérer les produits</a>
                    <?php endif; ?></li>    
            </ul>
        </nav>
        <div class="header-right">
            <button id="burger-btn">
                <i class="fa-solid fa-bars"></i>
            </button>
            <a href="#">
                <i id="cart-icon" class="fa-solid fa-cart-arrow-down"></i>
            </a>
        </div>
    </header>
    <main>
        <section id="product-section">
            <h2>Nos produits</h2>
            <div id="product-container">
                <?php foreach($products as $prod): ?>
                <div class="product_card">
                    <div class="img_product">
                        <img 
                            src="<?= htmlspecialchars($prod['image']) ?>" 
                            alt="<?= htmlspecialchars($prod['titre']) ?>"
                            class="js-open-product-modal"
                            data-title="<?= htmlspecialchars($prod['titre']) ?>"
                            data-description="<?= htmlspecialchars($prod['descriptif']) ?>"
                            data-price="<?= htmlspecialchars($prod['prix']) ?>"
                            data-image="<?= htmlspecialchars($prod['image']) ?>"
                        >
                    </div>
                    <h3><?= htmlspecialchars($prod['titre']) ?></h3>
                    <div class="info_product">
                        <p class="product-description"><?= htmlspecialchars($prod['descriptif']) ?></p>
                        <div class="price_and_cart"> 
                            <p class="product_price"><?= htmlspecialchars($prod['prix']) ?>€</p>
                            <a href="#" class="cart-icon">
                                <i class="fa-solid fa-cart-arrow-down"></i>
                            </a>
                        </div>
                    </div>
                </div>
                <?php endforeach; ?>
            </div>
        </section>
    </main>
    <footer>
        <ul>
            <li><a href="#">Contact</a></li>
            <li><a href="#">À propos</a></li>
            <li><a href="#">mentions légales</a></li>
        </ul>
    </footer>   
    <!-- MODAL PRODUIT ------------------------------------->
        <div id="product-view-modal" class="product-view-modal">
        <div class="product-view-modal__card">
            <button class="product-view-modal__close">&times;</button>

            <div class="img_product product-view-modal__img">
                <img id="product-view-image" src="" alt="">
            </div>

            <h3 id="product-view-title"></h3>

            <div class="info_product">
                <p class="product-description" id="product-view-description"></p>

                <div class="price_and_cart">
                    <p class="product_price" id="product-view-price"></p>

                    <button id="product-view-add-btn" class="product-view-add-btn">
                        Ajouter au panier
                    </button>

                    <p id="product-view-feedback" class="product-view-feedback">
                        Produit ajouté au panier (démo)
                    </p>
                </div>
            </div>
        </div>
    </div>
<script src="./assets/modalProduct.js"></script> 
<script src="./assets/menuBurger.js"></script> 
</body>
</html>